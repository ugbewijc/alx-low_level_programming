#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 *
 * @s: function parameter, datattype char, string to check
 *
 * Return: length of @s
 *
 *
 */

int _strlen(char *s)
{
	return (strlen(s));
}
